apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: minio-system
  name: minio-pvc
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
